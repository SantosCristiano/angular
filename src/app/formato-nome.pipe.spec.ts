import { FormatoNomePipe } from './formato-nome.pipe';

describe('FormatoNomePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoNomePipe();
    expect(pipe).toBeTruthy();
  });
});
